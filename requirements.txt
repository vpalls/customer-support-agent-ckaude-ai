# ── Core AI Framework ──
langchain>=0.3.14
langchain-anthropic>=0.3.0
langchain-community>=0.3.14
langchain-chroma>=0.2.0
langgraph>=0.2.64

# ── Embeddings (free, local — replaces OpenAI embeddings) ──
sentence-transformers>=3.0.0
huggingface-hub>=0.20.0

# ── Vector Store ──
chromadb>=0.5.0

# ── UI ──
chainlit>=1.3.0

# ── Utilities ──
pydantic>=2.0
python-dotenv>=1.0.0
tqdm>=4.60.0
